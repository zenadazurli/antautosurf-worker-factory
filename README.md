# AntAutoSurf Worker Factory


Crea automaticamente worker dedicati su Render per ogni account.

## 🚀 Come usare

1. **Prepara i file:**
   - `accounts.txt` (email:password)
   - `proxies.txt` (proxy in formato username:password@host:port)

2. **Imposta la variabile d'ambiente:**
   ```bash
   export RENDER_API_KEY="la_tua_chiave"